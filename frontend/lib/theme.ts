export const colors = {
  // 背景层级：从深到浅
  bg: "#1a1a2e",          // 最底层背景（深蓝黑）
  bgCard: "#16213e",      // 卡片/容器背景（稍浅一点）
  bgInput: "#0f3460",     // 输入框背景
  bgElevated: "#1a1a3e",  // 悬浮元素（modal 等）

  // 主色
  primary: "#e94560",     // 主操作色（红粉，用于主按钮、重点强调）
  primaryDark: "#c73e54",  // 按下/hover 状态

  // 文字
  text: "#ffffff",         // 主要文字
  textSecondary: "#a2a2b8", // 次要文字（副标题、说明）
  textMuted: "#6c6c80",    // 更淡的文字（placeholder 等）

  // 边框
  border: "#2a2a4a",       // 卡片边框
  borderLight: "#3a3a5a",  // 更亮的边框（输入框 focus 等）

  // 语义色
  danger: "#ff3b30",       // 删除、错误
  success: "#34c759",      // 成功
  warning: "#ff9500",      // 警告

  // Tab bar
  tabBg: "#16213e",
  tabActive: "#e94560",
  tabInactive: "#6c6c80",
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};

export const fontSize = {
  xs: 12,
  sm: 14,
  md: 16,
  lg: 18,
  xl: 24,
  xxl: 32,
  title: 40,
};

export const borderRadius = {
  sm: 8,
  md: 12,
  lg: 20,
  full: 50,
};